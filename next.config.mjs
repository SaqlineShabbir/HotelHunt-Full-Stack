/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["a0.muscache.com", "lh3.googleusercontent.com", "example.com"], // Add allowed domains here
  },
};

export default nextConfig;
